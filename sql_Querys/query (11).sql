CREATE TABLE Customer (
    CustomerID INT PRIMARY KEY,
    PersonID VARCHAR(50),
    StoreID VARCHAR(50),
    TerritoryID INT,
    AccountNumber VARCHAR(50),
    rowguid VARCHAR(50),
    ModifiedDate TIMESTAMP
);
